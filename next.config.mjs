/** @type {import('next').NextConfig} */
const nextConfig = {
   
        images: {
          domains: ['lh3.googleusercontent.com', 'i.ibb.co', 'avatars.githubusercontent.com'],
        },
    
};

export default nextConfig;
